This directory contains a snakemake workflow for running QC steps on STR VCF output files. To run, just type:

```
snakemake
```

To delete all outputs to force rerun:

```
snakemake --delete-all-output
```